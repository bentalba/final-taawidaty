# robots.txt for taawidaty.ma
# Last updated: 2025-11-16
# CRITICAL FIX: Removed fake medication URLs that were causing indexing issues

User-agent: *
Allow: /

# Block individual medication pages (these don't exist - SPA uses /prix-medicaments for search)
# This prevents Google from trying to index non-existent /prix/[medication-name] URLs
Disallow: /prix/

# Allow the actual price checker page
Allow: /prix-medicaments

# Sitemap (CLEAN VERSION - Only 13 real URLs)
Sitemap: https://taawidaty.ma/sitemap.xml
